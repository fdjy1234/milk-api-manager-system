conf:
  listen:
    port: 9000
  etcd:
    endpoints:
      - http://etcd:2379
    prefix: "/apisix"
  authentication:
    secret: milk-dashboard-secret-change-me  # IMPORTANT: Change this in production!

